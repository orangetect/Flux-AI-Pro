name: 🔍 Repository Diagnostics

on: [workflow_dispatch]

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: 📋 Repository Information
        run: |
          echo "=== 仓库诊断信息 ==="
          echo "当前仓库: ${{ github.repository }}"
          echo "仓库URL: https://github.com/${{ github.repository }}"
          echo "默认分支: ${{ github.event.repository.default_branch }}"
          echo "是否是Fork: ${{ github.event.repository.fork }}"
          
          if [ "${{ github.event.repository.fork }}" = "true" ]; then
            echo "✅ 这是一个Fork仓库"
            echo "父仓库: ${{ github.event.repository.parent.full_name }}"
            echo "父仓库URL: ${{ github.event.repository.parent.html_url }}"
          else
            echo "❌ 这不是Fork仓库，无需上游同步"
          fi
